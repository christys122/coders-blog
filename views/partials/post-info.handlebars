  <article class="post">
            <div class="title">
                <div "{{post_text}}">{{title}}</div>
                <span>({{post_text}})</span>
            </div>
                <div class="meta">
            |
            <a href="/post/{{id}}">{{comments.length}} comment(s)</a>   
            </div>
        </article>